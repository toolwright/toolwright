# Reference parameters for manual dogfood runs.
# CI workflows currently hardcode these paths directly.
# Source this file for local dev: source dogfood/jira/vars.env

# Jira Cloud instance (replace with your actual domain)
JIRA_DOMAIN=placeholder

# Auth: Basic auth with API token
# Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
# JIRA_EMAIL=your-email@example.com
# JIRA_API_TOKEN=<your-api-token>
# AUTH_HEADER="Authorization: Basic $(echo -n "$JIRA_EMAIL:$JIRA_API_TOKEN" | base64)"

# Toolpack and lockfile paths (relative to repo root)
TOOLPACK_PATH=dogfood/jira/toolpack.yaml
LOCKFILE_PATH=dogfood/jira/lockfile/toolwright.lock.yaml
BASELINE_PATH=dogfood/jira/artifact/baseline.json
